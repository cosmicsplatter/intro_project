<h1>Pokemon</h1>

<ul>
  <% @pokemons.each do |pokemon| %>
    <li>
      <h4><%=pokemon.name%></h4>
      <%=link_to image_tag(pokemon.sprite, :alt=>pokemon.name), pokemon%>
    </li>
  <% end %>
</ul>


<%= paginate @pokemons %>
